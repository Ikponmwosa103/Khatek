# Version control
.git
.gitignore
.github

# Docker
Dockerfile
.dockerignore
docker-entrypoint.sh

# PHP dependencies (reinstall in container if needed; avoids stale vendor)
vendor/

# JS / frontend
node_modules/
npm-debug.log
yarn-error.log
package-lock.json
yarn.lock
dist/
build/

# Env & secrets - NEVER copy .env into image; use Railway variables
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# Logs & caches
*.log
storage/logs/
storage/framework/cache/
storage/framework/sessions/
storage/framework/views/
bootstrap/cache/

# Uploads (persist via volume, not image)
public/uploads/*

# Tests & CI not needed in prod image
tests/
phpunit.xml
README.md
